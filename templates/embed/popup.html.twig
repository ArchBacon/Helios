{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <div class="fixed top-0 right-0 h-12 bg-zinc-600 w-full shadow-lg flex items-center px-12">
        {% include 'shared/breadcrumb.html.twig' with {
            'page_name': 'Generate embed',
        } only %}
    </div>

    <div class="pt-12">
        <pre class="p-4 bg-slate-800 rounded-xl shadow-lg ring-1 ring-slate-600 inline-block"><code>{% include 'embed/embed_codeblock.html.twig' %}</code></pre>
    </div>
{% endblock %}